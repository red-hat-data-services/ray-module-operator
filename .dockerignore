# More info: https://docs.docker.com/engine/reference/builder/#dockerignore-file
# Ignore everything by default and re-include only needed files
**

# Re-include directories needed for the build. Without this, buildah/podman
# (unlike BuildKit) treats these as fully excluded by the "**" rule above and
# fails COPY with "no such file or directory" even though the *.go negation
# below re-includes files inside them.
!cmd/
!api/
!internal/

# Re-include Go source files (but not *_test.go)
!**/*.go
**/*_test.go

# Re-include Go module files
!go.mod
!go.sum

# Module-owned KubeRay snapshots. The Dockerfile selects the platform tree.
!opt/
!opt/manifests/
!opt/manifests/odh/
!opt/manifests/odh/kuberay/
!opt/manifests/odh/kuberay/**
!opt/manifests/rhoai/
!opt/manifests/rhoai/kuberay/
!opt/manifests/rhoai/kuberay/**

# Re-include embedded manifests for go:embed
!internal/controller/manifests/
